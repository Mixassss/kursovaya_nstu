1.  У вас есть следующий код для реализации стека. Какой из следующих вариантов \nправильно завершает функцию push, чтобы добавить элемент на вершину стека?

    void push(Node **top, int d) {
        Node *pv = new Node;    
        pv->d = d;
        pv->p = *top;                             
                    ; // Дополните эту строку
    }

    pv->p = 0
    *top = pv
    pv->d = *top
    *top = pv->p

Правильный ответ: *top = pv

2. Дополните следующий код, выбрав правильный вариант, чтобы функция strlen корректно подсчитывала длину строки.

    int strlen(char* p) {    
        int i = 0;    
        while (         ) {        
            i++;   
        }   
        return i;
    }

    *p[i] != '\0'
    p != '\0'
    p[i] != '\0'
    *p != 0

Правильный ответ: *p != 0

3.Какой из следующих фрагментов кода будет выполняться как минимум один раз, если переменная x равна 0?

    A) while (x > 0) { cout << x; }
    B) while (x < 0) { cout << x; }
    C) do { cout << x; } while (x > 0);
    D) do { cout << x; } while (x < 0);

Правильный ответ: C) do { cout << x; } while (x > 0);