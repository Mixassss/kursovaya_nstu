<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>all_lection</class>
 <widget class="QDialog" name="all_lection">
  <property name="geometry">
   <rect>
    <x>0</x>
    <y>0</y>
    <width>969</width>
    <height>491</height>
   </rect>
  </property>
  <property name="windowTitle">
   <string>Обучающая система</string>
  </property>
  <widget class="QScrollArea" name="scrollArea">
   <property name="geometry">
    <rect>
     <x>-1</x>
     <y>49</y>
     <width>971</width>
     <height>381</height>
    </rect>
   </property>
   <property name="widgetResizable">
    <bool>true</bool>
   </property>
   <widget class="QWidget" name="scrollAreaWidgetContents">
    <property name="geometry">
     <rect>
      <x>0</x>
      <y>0</y>
      <width>1000</width>
      <height>6338</height>
     </rect>
    </property>
    <property name="minimumSize">
     <size>
      <width>1000</width>
      <height>6338</height>
     </size>
    </property>
    <widget class="QWidget" name="widget" native="true">
     <property name="geometry">
      <rect>
       <x>0</x>
       <y>0</y>
       <width>1201</width>
       <height>6341</height>
      </rect>
     </property>
     <property name="styleSheet">
      <string notr="true">background-color: rgb(94, 92, 100);</string>
     </property>
     <widget class="QLabel" name="label">
      <property name="geometry">
       <rect>
        <x>0</x>
        <y>0</y>
        <width>1341</width>
        <height>6341</height>
       </rect>
      </property>
      <property name="text">
       <string>&lt;html&gt;&lt;head/&gt;&lt;body&gt;&lt;p&gt;&lt;span style=&quot; font-size:12pt; font-weight:700;&quot;&gt;Область видимости, объекты и адреса&lt;/span&gt;&lt;/p&gt;&lt;p&gt;Описанием определяется область видимости имени. Это значит, что имя может использоваться только в определенной части текста &lt;/p&gt;&lt;p&gt;программы. Если имя описано в функции (обычно его называют локальным именем), то область видимости имени простирается от точки &lt;/p&gt;&lt;p&gt;описания до конца блока, в котором появилось это описание. Если имя не находится в описании функции или класса &lt;/p&gt;&lt;p&gt;(его обычно называют глобальным именем), то область видимости &lt;/p&gt;&lt;p&gt;простирается от точки описания до конца файла, в котором появилось это описание.&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;Описание имени в блоке может скрывать описание в объемлющем блоке или глобальное имя; &lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;т.е. имя может быть переопределено так, что оно будет обозначать другой объект внутри блока. &lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;После выхода из блока прежнее значение имени (если оно было) восстанавливается. &lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;Описание имени: Это процесс объявления переменной, функции или другого объекта, который позволяет программе использовать &lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;это имя для доступа к ресурсу в памяти.&lt;/p&gt;&lt;p&gt;Приведем пример:&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;int x; // глобальное x&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;void f() {&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;int x; // локальное x скрывает глобальное x&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;x = 1; { // присвоить локальному x&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;int x; // скрывает первое локальное x&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;x = 2; // присвоить второму локальному x&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;}&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;x = 3; // присвоить первому локальному x&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;}&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;int* p = &amp;amp;x; // взять адрес глобального x&lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;Область видимости имени начинается в точке его описания (по окончании описателя, но еще до начала инициализатора). &lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;Области видимости: &lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;Важно понимать, что область видимости может зависеть от контекста, в котором объявлено имя, и может влиять на то, &lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;как и где это имя может быть использовано. &lt;/p&gt;&lt;p align=&quot;justify&quot;&gt;Это означает, что имя можно использовать даже до того, как задано его начальное значение. &lt;/p&gt;&lt;p&gt;Например:&lt;/p&gt;&lt;p&gt;int x;&lt;/p&gt;&lt;p&gt;void f3() {&lt;/p&gt;&lt;p&gt;int x = x; // ошибочное присваивание&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;Можно выделять память для переменных, не имеющих имен, и использовать эти переменные. &lt;/p&gt;&lt;p&gt;Возможно даже присваивание таким странно выглядящим переменным, например, *p[a+10]=7.&lt;/p&gt;&lt;p&gt;Следовательно, есть потребность именовать нечто хранящееся в памяти. Можно привести подходящую цитату из справочного руководства: &lt;/p&gt;&lt;p&gt;Любой объект - это некоторая область памяти, а адресом называется выражение, ссылающееся на объект или функцию&lt;/p&gt;&lt;p&gt;Константы: Константы представляют собой фиксированные значения, которые не могут быть изменены &lt;/p&gt;&lt;p&gt;в процессе выполнения программы. Они помогают предотвратить случайные изменения значений и делают код более понятным.&lt;/p&gt;&lt;p&gt;&lt;span style=&quot; font-size:12pt; font-weight:700;&quot;&gt;Имена, типы и указатели&lt;/span&gt;&lt;/p&gt;&lt;p&gt;Имя (идентификатор) является последовательностью букв или цифр. Первый символ должен быть буквой. &lt;/p&gt;&lt;p&gt;Буквой считается и символ подчеркивания _. В реализацию входят программные компоненты, &lt;/p&gt;&lt;p&gt;которыми создатель транслятора управлять не может, а они могут устанавливать ограничения. &lt;/p&gt;&lt;p&gt;Идентификаторы: Имена (идентификаторы) могут состоять из букв, цифр и символа подчеркивания, &lt;/p&gt;&lt;p&gt;но первый символ должен быть буквой или символом подчеркивания. Служебные слова не могут использоваться в качестве идентификаторов.&lt;/p&gt;&lt;p&gt;Нельзя использовать в качестве имен служебные слова С++, например:&lt;/p&gt;&lt;p&gt;hello this_is_a_most_unusially_long_name&lt;/p&gt;&lt;p&gt;DEFINED foO bAr u_name HorseSense&lt;/p&gt;&lt;p&gt;var0 var1 CLASS _class ___&lt;/p&gt;&lt;p&gt;Теперь приведем примеры последовательностей символов, которые не могут использоваться как идентификаторы:&lt;/p&gt;&lt;p&gt;012 a fool $sys class 3var&lt;/p&gt;&lt;p&gt;pay.due foo~bar .name if&lt;/p&gt;&lt;p&gt;Заглавные и строчные буквы считаются различными, поэтому Count и count - разные имена. &lt;/p&gt;&lt;p&gt;Но выбирать имена, почти не отличающиеся друг от друга, неразумно.&lt;/p&gt;&lt;p&gt;Основные типы С++ представляют самые распространенные единицы памяти машин и все основные способы работы с ними. Это:&lt;/p&gt;&lt;p&gt;char&lt;/p&gt;&lt;p&gt;short int&lt;/p&gt;&lt;p&gt;int&lt;/p&gt;&lt;p&gt;long int&lt;/p&gt;&lt;p&gt;Перечисленные типы используются для представления различного размера целых. Числа с плавающей точкой представлены типами:&lt;/p&gt;&lt;p&gt;float&lt;/p&gt;&lt;p&gt;double&lt;/p&gt;&lt;p&gt;long double&lt;/p&gt;&lt;p&gt;Следующие типы могут использоваться для представления без знаковых целых, логических значений, разрядных массивов и т.д.&lt;/p&gt;&lt;p&gt;unsigned char&lt;/p&gt;&lt;p&gt;unsigned short int&lt;/p&gt;&lt;p&gt;unsigned int&lt;/p&gt;&lt;p&gt;unsigned long int&lt;/p&gt;&lt;p&gt;Ниже приведены типы, которые используются для явного задания знаковых типов:&lt;/p&gt;&lt;p&gt;signed char&lt;/p&gt;&lt;p&gt;signed short int&lt;/p&gt;&lt;p&gt;signed int&lt;/p&gt;&lt;p&gt;signed long int&lt;/p&gt;&lt;p&gt;Для большинства типов T указатель на T имеет тип T*. Это значит, что переменная типа T* &lt;/p&gt;&lt;p&gt;может хранить адрес объекта типа T. Типы данных: В C++ существуют различные типы данных для представления целых чисел, &lt;/p&gt;&lt;p&gt;чисел с плавающей точкой и знаковых/беззнаковых типов. К основным типам относятся char, int, float, double и их вариации. &lt;/p&gt;&lt;p&gt;Указатели на массивы и функции, к сожалению, требуют более сложной записи:&lt;/p&gt;&lt;p&gt;int* pi;&lt;/p&gt;&lt;p&gt;char** cpp; // указатель на указатель на char&lt;/p&gt;&lt;p&gt;int (*vp)[10]; // указатель на массив из 10 целых&lt;/p&gt;&lt;p&gt;int (*fp)(char, char*); // указатель на функцию с параметрами // char и char*, возвращающую int. &lt;/p&gt;&lt;p&gt;Главная операция над указателями - это косвенное обращение (разыменование), т.е. обращение к объекту, &lt;/p&gt;&lt;p&gt;на который настроен указатель. Эту операцию обычно называют просто косвенностью. Операция косвенности * &lt;/p&gt;&lt;p&gt;является префиксной унарной операцией. &lt;/p&gt;&lt;p&gt;Например:&lt;/p&gt;&lt;p&gt;char c1 = 'a';&lt;/p&gt;&lt;p&gt;char* p = &amp;amp;c1; // p содержит адрес c1&lt;/p&gt;&lt;p&gt;char c2 = *p; // c2 = 'a'&lt;/p&gt;&lt;p&gt;Указатели в C++ позволяют хранить адреса объектов и выполнять операции разыменования для доступа к значениям этих объектов. &lt;/p&gt;&lt;p&gt;Указатели на массивы и функции имеют более сложный синтаксис. Переменная, на которую указывает p,&lt;/p&gt;&lt;p&gt;- это c1, а значение, которое хранится в c1, равно 'a'. Поэтому присваиваемое c2 значение *p есть 'a'. &lt;/p&gt;&lt;p&gt;Над указателями можно выполнять и некоторые арифметические операции.&lt;/p&gt;&lt;p&gt;Ниже в качестве примера представлена функция, подсчитывающая число символов в строке, заканчивающейся &lt;/p&gt;&lt;p&gt;нулевым символом (который не учитывается):&lt;/p&gt;&lt;p&gt;int strlen(char* p) {&lt;/p&gt;&lt;p&gt;int i = 0;&lt;/p&gt;&lt;p&gt;while (*p++) i++;&lt;/p&gt;&lt;p&gt;return i;&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;&lt;span style=&quot; font-size:12pt; font-weight:700;&quot;&gt;Структуры&lt;/span&gt;&lt;/p&gt;&lt;p&gt;Массив представляет собой совокупность элементов одного типа, а структура является совокупностью элементов произвольных &lt;/p&gt;&lt;p&gt;(практически) типов. Структуры в C++ представляют собой пользовательские типы данных, которые могут содержать &lt;/p&gt;&lt;p&gt;переменные различных типов. &lt;/p&gt;&lt;p&gt;Они позволяют группировать связанные данные, что упрощает организацию и управление данными.&lt;/p&gt;&lt;p&gt;Например:&lt;/p&gt;&lt;p&gt;struct address {&lt;/p&gt;&lt;p&gt;char* name; // имя Jim Dandy&lt;/p&gt;&lt;p&gt;long number; // номер дома 61&lt;/p&gt;&lt;p&gt;char* street; // улица South Street&lt;/p&gt;&lt;p&gt;char* town; // город New Providence&lt;/p&gt;&lt;p&gt;char* state[2]; // штат 'N' 'J'&lt;/p&gt;&lt;p&gt;int zip; // индекс 7974&lt;/p&gt;&lt;p&gt;};&lt;/p&gt;&lt;p&gt;После определения структуры можно создавать объекты этого типа, присваивать им значения &lt;/p&gt;&lt;p&gt;и передавать их в функции. &lt;/p&gt;&lt;p&gt;Однако операции сравнения между структурами не определены по умолчанию, и программист &lt;/p&gt;&lt;p&gt;может реализовать их самостоятельно. &lt;/p&gt;&lt;p&gt;Объекты структурного типа могут быть присвоены, переданы как фактические параметры функций &lt;/p&gt;&lt;p&gt;и возвращены функциями в качестве результата.&lt;/p&gt;&lt;p&gt;Например:&lt;/p&gt;&lt;p&gt;address current;&lt;/p&gt;&lt;p&gt;address set_current(address next) {&lt;/p&gt;&lt;p&gt;address prev = current;&lt;/p&gt;&lt;p&gt;current = next;&lt;/p&gt;&lt;p&gt;return prev;&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;Другие допустимые операции, например, такие, как сравнение (== и !=), неопределены.&lt;/p&gt;&lt;p&gt;Однако пользователь может сам определить эти операции. Размер объекта структурного типа &lt;/p&gt;&lt;p&gt;не обязательно равен сумме размеров всех его членов. &lt;/p&gt;&lt;p&gt;Типичный пример - это выравнивание целого по словной границе. В результате выравнивания &lt;/p&gt;&lt;p&gt;могут появиться «дырки» в структуре. &lt;/p&gt;&lt;p&gt;Так, на уже упоминавшейся машине автора sizeof(address) равно 24, а не 22, как можно было ожидать.&lt;/p&gt;&lt;p&gt;Следует также упомянуть, что тип можно использовать сразу после его появления в описании, еще до того, &lt;/p&gt;&lt;p&gt;как будет завершено все описание.&lt;/p&gt;&lt;p&gt;Например:&lt;/p&gt;&lt;p&gt;struct link {&lt;/p&gt;&lt;p&gt;link* previous;&lt;/p&gt;&lt;p&gt;link* successor;&lt;/p&gt;&lt;p&gt;};&lt;/p&gt;&lt;p&gt;Однако новые объекты типа структуры нельзя описать до тех пор, пока не появится ее полное описание.&lt;/p&gt;&lt;p&gt;Поэтому описание &lt;/p&gt;&lt;p&gt;struct no_good {&lt;/p&gt;&lt;p&gt;no_good member;&lt;/p&gt;&lt;p&gt;};&lt;/p&gt;&lt;p&gt;является ошибочным (транслятор не в состоянии установить размер no_good).&lt;/p&gt;&lt;p&gt;&lt;span style=&quot; font-size:12pt; font-weight:700;&quot;&gt;Функции&lt;/span&gt;&lt;/p&gt;&lt;p&gt;Функция - это поименованная часть программы, которая может вызываться из других частей программы столько раз, &lt;/p&gt;&lt;p&gt;сколько необходимо. Функция состоит из заголовка и тела. Заголовок включает имя функции, &lt;/p&gt;&lt;p&gt;тип возвращаемого значения и параметры. &lt;/p&gt;&lt;p&gt;Тело функции содержит инструкции, которые будут выполняться при вызове функции.&lt;/p&gt;&lt;p&gt;Приведем программу, выдающую степени числа два:&lt;/p&gt;&lt;p&gt;extern float pow ( float, int ); // pow () определена в другом месте&lt;/p&gt;&lt;p&gt;int main () {&lt;/p&gt;&lt;p&gt;for ( int i=0; i&amp;lt;10; i++ ) cout &amp;lt;&amp;lt; pow ( 2, i ) &amp;lt;&amp;lt; endl;&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;Первая строка является описанием функции. Она задает pow как функцию с параметрами типа float и int, &lt;/p&gt;&lt;p&gt;возвращающую значение типа float. Описание функции необходимо для ее вызова, ее определение находится в другом месте. &lt;/p&gt;&lt;p&gt;При вызове функции тип каждого фактического параметра сверяется с типом, указанным в описании функции, &lt;/p&gt;&lt;p&gt;точно так же, как если бы инициализировалась переменная описанного типа.&lt;/p&gt;&lt;p&gt;Это гарантирует надлежащую проверку и преобразования типов. &lt;/p&gt;&lt;p&gt;Например, вызов функции pow(12.3,«abcd») транслятор сочтет ошибочным, &lt;/p&gt;&lt;p&gt;поскольку «abcd» является строкой, а не параметром типа int. В вызове pow(2,i) транслятор преобразует целую константу &lt;/p&gt;&lt;p&gt;(целое 2) в число с плавающей точкой (float), как того требует функция.&lt;/p&gt;&lt;p&gt;Функция pow может быть определена следующим образом:&lt;/p&gt;&lt;p&gt;float pow ( float x, int n ) {&lt;/p&gt;&lt;p&gt;if ( n &amp;lt; 0 )&lt;/p&gt;&lt;p&gt;error ( ошибка: для pow () задан отрицательный показатель);&lt;/p&gt;&lt;p&gt;switch ( n ) {&lt;/p&gt;&lt;p&gt;case 0: return 1;&lt;/p&gt;&lt;p&gt;case 1: return x;&lt;/p&gt;&lt;p&gt;default: return x * pow ( x, n-1 );&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;Первая часть определения функции задает ее имя, тип возвращаемого значения (если оно есть), &lt;/p&gt;&lt;p&gt;а также типы и имена формальных параметров (если они существуют). &lt;/p&gt;&lt;p&gt;Значение возвращается из функции с помощью оператора «return».&lt;/p&gt;&lt;p&gt;Разные функции обычно имеют разные имена, но функциям, выполняющим сходные операции над объектами разных типов, &lt;/p&gt;&lt;p&gt;лучше дать одно имя. Если типы параметров таких функций различны, то транслятор всегда может разобраться, &lt;/p&gt;&lt;p&gt;какую функцию нужно вызывать. Например, можно иметь две функции возведения в степень: &lt;/p&gt;&lt;p&gt;одну - для целых чисел, а другую - для чисел с плавающей точкой:&lt;/p&gt;&lt;p&gt;int pow ( int, int );&lt;/p&gt;&lt;p&gt;double pow ( double, double );&lt;/p&gt;&lt;p&gt;//...&lt;/p&gt;&lt;p&gt;x = pow ( 2,10 ); // вызов pow ( int, int )&lt;/p&gt;&lt;p&gt;y = pow ( 2.0, 10.0 );// вызов pow ( double, double )&lt;/p&gt;&lt;p&gt;Такое многократное использование имени называется перегрузкой имени функции или просто перегрузкой. &lt;/p&gt;&lt;p&gt;Функции могут иметь одно и то же имя, но разные параметры (по количеству или типам). &lt;/p&gt;&lt;p&gt;Это позволяет использовать одно имя для выполнения различных операций.&lt;/p&gt;&lt;p&gt;Параметры функции могут передаваться либо по значению, либо по ссылке. Передача по значению создает копию аргумента, &lt;/p&gt;&lt;p&gt;а передача по ссылке позволяет функции изменять оригинальный аргумент. Рассмотрим определение функции, &lt;/p&gt;&lt;p&gt;которая осуществляет взаимообмен значений двух целых переменных.&lt;/p&gt;&lt;p&gt;Если используется стандартный способ передачи параметров по значению, то придется передавать указатели:&lt;/p&gt;&lt;p&gt;void swap ( int *p, int *q ) {&lt;/p&gt;&lt;p&gt;int t = * p;&lt;/p&gt;&lt;p&gt;*p = *q;&lt;/p&gt;&lt;p&gt;*q = t;&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;Унарная операция * называется косвенностью (или операцией разыменования), &lt;/p&gt;&lt;p&gt;она выбирает значение объекта, на который настроен указатель.&lt;/p&gt;&lt;p&gt;Функцию можно вызывать следующим образом:&lt;/p&gt;&lt;p&gt;void f ( int i, int j ) {&lt;/p&gt;&lt;p&gt;swap ( &amp;amp; i, &amp;amp; j );&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;Если использовать передачу параметра по ссылке, можно обойтись без явных операций с указателем:&lt;/p&gt;&lt;p&gt;void swap (int &amp;amp; r1, int &amp;amp; r2 ) {&lt;/p&gt;&lt;p&gt;int t = r1;&lt;/p&gt;&lt;p&gt;r1 = r2;&lt;/p&gt;&lt;p&gt;r2 = t;&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;p&gt;void g ( int i, int j ) {&lt;/p&gt;&lt;p&gt;swap ( i, j );&lt;/p&gt;&lt;p&gt;}&lt;/p&gt;&lt;/body&gt;&lt;/html&gt;</string>
      </property>
     </widget>
    </widget>
   </widget>
  </widget>
  <widget class="QPushButton" name="pushButton">
   <property name="geometry">
    <rect>
     <x>400</x>
     <y>440</y>
     <width>161</width>
     <height>41</height>
    </rect>
   </property>
   <property name="text">
    <string>Вернуться к лекциям</string>
   </property>
  </widget>
  <widget class="QWidget" name="widget_2" native="true">
   <property name="geometry">
    <rect>
     <x>-20</x>
     <y>0</y>
     <width>991</width>
     <height>51</height>
    </rect>
   </property>
   <property name="styleSheet">
    <string notr="true">background-color: rgb(224, 27, 36);</string>
   </property>
   <widget class="QLabel" name="label_2">
    <property name="geometry">
     <rect>
      <x>20</x>
      <y>0</y>
      <width>911</width>
      <height>51</height>
     </rect>
    </property>
    <property name="font">
     <font>
      <family>LM Roman 8</family>
      <pointsize>20</pointsize>
      <bold>true</bold>
     </font>
    </property>
    <property name="text">
     <string>Лекция №1. Описания и константы</string>
    </property>
   </widget>
  </widget>
 </widget>
 <resources/>
 <connections/>
</ui>
